# Go build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.txt
coverage.html

# Go workspace
go.work
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code local settings (per-developer)
.claude/settings.local.json

# MCP server registrations contain absolute paths to local binaries
# (per-developer). Re-create from spikes/s2-mcp-sdk/README.md when needed.
.mcp.json

# Build outputs
/bin/
/agent-memory
/agent-memory.exe

# Spike scratch outputs
spikes/**/scratch/

# Behavioural-eval transcripts (DEBUG=1 dumps; per-developer)
behavioural-transcripts/

# Local .agent-memory dogfooding (when we dogfood the project on itself)
.agent-memory/meta/index.sqlite
.agent-memory/meta/index.sqlite-*
.agent-memory/meta/lock
.agent-memory/local/
.agent-memory/sessions/
.agent-memory/staging/
